# B093a: Define a project-selection snapshot for `use` and default-command fallback behavior.

## Dependencies

B015b,B016b

## Owned paths

`internal/cli/**`, `internal/context/**`, `tests/cli/**`

## Acceptance

Define a project-selection snapshot for `use` and default-command fallback behavior.

## Rules

- No placeholder code, no TODO comments, no partial implementations.
- Add tests in the same task.
- Final gates: `bin/test` and `bin/lint` must both pass.
- Commit with conventional commit wording after gates pass.
- Move this file to `tasks/done/` as part of the same commit.
