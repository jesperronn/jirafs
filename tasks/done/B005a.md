# B005a: Add `main` command tests for `mirror` routing and unknown subcommand/help edge cases to raise binary entrypoint coverage.

## Dependencies

B080b

## Owned paths

`cmd/jirafs/**`, `tests/**`

## Acceptance

Add `main` command tests for `mirror` routing and unknown subcommand/help edge cases to raise binary entrypoint coverage.

## Rules

- No placeholder code, no TODO comments, no partial implementations.
- Add tests in the same task.
- Final gates: `bin/test` and `bin/lint` must both pass.
- Commit with conventional commit wording after gates pass.
- Move this file to `tasks/done/` as part of the same commit.
