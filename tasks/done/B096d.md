# B096d: Add board command coverage for `--group-by` and `--project` behavior.

## Dependencies

B096c,B093b

## Owned paths

`cmd/jirafs/**`, `internal/cli/**`, `tests/cli/**`

## Acceptance

Add board command coverage for `--group-by` and `--project` behavior, including invalid combinations and the default grouping fallback.

## Rules

- No placeholder code, no TODO comments, no partial implementations.
- Add tests in the same task.
- Final gates: `bin/test` and `bin/lint` must both pass.
- Commit with conventional commit wording after gates pass.
- Move this file to `tasks/done/` as part of the same commit.
