# B005c: Add Jira client tests for auth/header construction and search/fetch error branches that still miss coverage.

## Dependencies

B054a,B055b

## Owned paths

`internal/jira/**`, `tests/**`

## Acceptance

Add Jira client tests for auth/header construction and search/fetch error branches that still miss coverage.

## Rules

- No placeholder code, no TODO comments, no partial implementations.
- Add tests in the same task.
- Final gates: `bin/test` and `bin/lint` must both pass.
- Commit with conventional commit wording after gates pass.
- Move this file to `tasks/done/` as part of the same commit.
