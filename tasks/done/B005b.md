# B005b: Add focused `mirror` CLI tests for refresh/archive argument errors, project-resolution failures, and persistence edge cases.

## Dependencies

B081b

## Owned paths

`internal/cli/**`, `tests/**`

## Acceptance

Add focused `mirror` CLI tests for refresh/archive argument errors, project-resolution failures, and persistence edge cases.

## Rules

- No placeholder code, no TODO comments, no partial implementations.
- Add tests in the same task.
- Final gates: `bin/test` and `bin/lint` must both pass.
- Commit with conventional commit wording after gates pass.
- Move this file to `tasks/done/` as part of the same commit.
