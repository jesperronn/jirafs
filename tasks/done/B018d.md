# B018d: Setup helper can set the remembered current project so the first export/refresh flow works without extra flags.

## Dependencies

B018c,B080a

## Owned paths

`cmd/jirafs/**`, `internal/context/**`, `tests/**`

## Acceptance

Setup helper can set the remembered current project so the first export/refresh flow works without extra flags.

## Rules

- No placeholder code, no TODO comments, no partial implementations.
- Add tests in the same task.
- Final gates: `bin/test` and `bin/lint` must both pass.
- Commit with conventional commit wording after gates pass.
- Move this file to `tasks/done/` as part of the same commit.
