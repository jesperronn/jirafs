# B031b: Populate `Issue.Sections` for `Description` and `Acceptance Criteria`, including empty sections.

## Dependencies

B031a

## Owned paths

`internal/codec/**`, `tests/codec/**`

## Acceptance

Populate `Issue.Sections` for `Description` and `Acceptance Criteria`, including empty sections.

## Rules

- No placeholder code, no TODO comments, no partial implementations.
- Add tests in the same task.
- Final gates: `bin/test` and `bin/lint` must both pass.
- Commit with conventional commit wording after gates pass.
- Move this file to `tasks/done/` as part of the same commit.
