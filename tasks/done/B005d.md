# B005d: Add schema parse/render round-trip tests for zero-value, partial-metadata, and invalid-state edge cases.

## Dependencies

B033b

## Owned paths

`internal/schema/**`, `tests/**`

## Acceptance

Add schema parse/render round-trip tests for zero-value, partial-metadata, and invalid-state edge cases.

## Rules

- No placeholder code, no TODO comments, no partial implementations.
- Add tests in the same task.
- Final gates: `bin/test` and `bin/lint` must both pass.
- Commit with conventional commit wording after gates pass.
- Move this file to `tasks/done/` as part of the same commit.
