# B018a: Add a setup helper that records Jira base URL, mirror directory, and auth source for one named project.

## Dependencies

B017h

## Owned paths

`cmd/jirafs/**`, `internal/config/**`, `tests/**`

## Acceptance

Add a setup helper that records Jira base URL, mirror directory, and auth source for one named project.

## Rules

- No placeholder code, no TODO comments, no partial implementations.
- Add tests in the same task.
- Final gates: `bin/test` and `bin/lint` must both pass.
- Commit with conventional commit wording after gates pass.
- Move this file to `tasks/done/` as part of the same commit.
