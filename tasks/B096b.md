# B096b: Group board issues by assignee and epic with deterministic ordering.

## Dependencies

B096a,B091a

## Owned paths

`internal/board/**`, `tests/board/**`

## Acceptance

Group board issues by assignee and epic with deterministic ordering and clear fallback behavior for unassigned or missing values.

## Rules

- No placeholder code, no TODO comments, no partial implementations.
- Add tests in the same task.
- Final gates: `bin/test` and `bin/lint` must both pass.
- Commit with conventional commit wording after gates pass.
- Move this file to `tasks/done/` as part of the same commit.
